# ============================================
# 1. Java & Maven (Build output)
# ============================================
target/
*.class
*.jar
*.war
*.ear
hs_err_pid*
dependency-reduced-pom.xml

# ============================================
# 2. IDE & Editors (Cấu hình riêng của máy dev)
# ============================================
.idea/
*.iml
*.ipr
*.iws
*.classpath
*.project
*.settings/
.vscode/
.nb-gradle/

# ============================================
# 3. OS Files (File rác hệ điều hành)
# ============================================
.DS_Store
Thumbs.db
Desktop.ini

# ============================================
# 4. Logs (File nhật ký chạy)
# ============================================
logs/
*.log
*.log.*

# ============================================
# 5. SECRETS & ENVIRONMENT (QUAN TRỌNG NHẤT)
# ============================================
# Chặn tất cả các file .env (Chứa mật khẩu thật)
.env
.env.*

# Chặn file secret cũ (để phòng hờ nếu bạn chưa xóa file này ở local)
src/main/resources/application-secret.yml
src/main/resources/application-secret.properties

# Các file key bảo mật khác
*.key
*.pem
*.p12
*.jks

# ============================================
# 6. Docker & Database Data
# ============================================
# Nếu bạn mount dữ liệu DB ra thư mục local thì phải chặn nó lại
# (Tránh commit cả cục database vài GB lên Git)
postgres_data/
mysql_data/
docker-compose.override.yml

# ============================================
# 7. Frontend (Nếu có folder frontend trong project)
# ============================================
node_modules/
dist/
build/
coverage/
.npm/