global:
  scrape_interval: 5s # Cứ 5 giây đi lấy dữ liệu một lần (thực tế thường để 15s-30s)

scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080'] # Địa chỉ App Spring Boot của bạn